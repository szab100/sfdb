# Gazelle (golang support tool)
load("@bazel_gazelle//:def.bzl", "gazelle")

gazelle(
    name = "gazelle",
    prefix = "github.com/googlegsa/sfdb",
)

# Directive to disable automatic go_proto_library generating
# gazelle:proto disable
